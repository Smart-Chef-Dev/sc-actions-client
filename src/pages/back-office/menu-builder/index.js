import { memo } from "react";

import { Flex } from "components/flex";

const MenuBuilder = () => {
  return (
    <Flex>
      <p>Menu builder</p>
    </Flex>
  );
};

export default memo(MenuBuilder);
