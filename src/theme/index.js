const spacing = ["0rem", "1rem", "2rem", "3rem", "4rem", "5rem"];
const fontSize = ["13px", "18px", "22px", "28px"];

export const theme = {
  spacing: (index) => spacing[index],
  fontSize: (index) => fontSize[index],
};
